<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.library.mapper.CategoryMapper">

    <resultMap id="CategoryResultMap" type="com.example.library.entity.Category">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="parentId" column="parent_id"/>
        <result property="sort" column="sort"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="findAll" resultMap="CategoryResultMap">
        SELECT id, name, parent_id, sort, create_time, update_time
        FROM t_category
        ORDER BY sort ASC, id ASC
    </select>

    <select id="findById" parameterType="java.lang.Long" resultMap="CategoryResultMap">
        SELECT id, name, parent_id, sort, create_time, update_time
        FROM t_category
        WHERE id = #{id}
    </select>

</mapper>